/**
 * In Kotlin, for loop is used to iterate through ranges, arrays,
 * maps and so on (anything that provides an iterator).
 */
fun main() {

    val arr = arrayOf(2,3,8,9,6,10)

    for(element in arr) {
        println(element)
    }

}